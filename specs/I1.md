# Task I1 — fly.io deploy config
Owns (exclusive): `deploy/Dockerfile`, `fly.toml`, `.dockerignore`
Depends on: contract only (build commands are in root package.json; server serves client/dist —
see CONTRACT.md server/src/index.ts and README.md).

Read first: README.md, root package.json scripts, server/package.json, CONTRACT.md server entry.

Implement:
- deploy/Dockerfile (fly.io expects it at deploy/Dockerfile via fly.toml dockerfile path):
  multi-stage node:22-alpine — stage 1: copy package.json + package-lock.json + all workspace
  package.jsons, npm ci; copy source, npm run build (client vite build + server esbuild bundle);
  stage 2: production node_modules only (npm ci --omit=dev with the workspaces' prod deps: ws),
  copy server/dist + client/dist + shared/src (only if the server bundle references it — esbuild
  bundles shared, so it should not; verify), ENV PORT=8080, EXPOSE 8080, CMD ["node",
  "server/dist/server.js"]. Keep image < 250MB.
- fly.toml: app "stricken-fps", primary_region "iad", build.dockerfile "deploy/Dockerfile",
  [[services]] internal_port 8080, protocol tcp, http checks on / (GET 200), auto_stop_machines
  'stop', auto_start_machines true, min_machines_running 0, [[vm]] size "shared-cpu-1x" memory 512.
- .dockerignore: node_modules, dist, .git, screenshots, specs, *.md except README.

Do NOT run flyctl or deploy anything. Do not modify package.json files.
Definition of done: files exist and are internally consistent with the repo layout (verify the
Dockerfile's COPY paths against actual directories); typecheck unaffected.
Return: summary, files touched, any assumptions, risks. NOT file contents.
