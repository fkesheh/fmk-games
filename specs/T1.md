# Task T1 — unit tests: physics, protocol, game logic
Owns (exclusive): `shared/src/physics.test.ts`, `shared/src/protocol.test.ts`,
`server/src/game.test.ts`
Depends on: contract only. Write against the frozen APIs — game.test.ts imports
`../src/game.js`'s GameRoom per the CONTRACT.md module table (it may not exist yet; it will).

Read first: CONTRACT.md (RULES + module tables S2/S3 + debug/test surface), shared/src/physics.ts,
protocol.ts, config.ts, maps/dustbowl.ts.

Write vitest suites (meaningful, strict assertions — no tautologies):
- physics.test.ts:
  - stepBody walks forward at speedRun (displacement per 30 ticks ≈ 4.8m × moveMul)
  - blocked by a wall: no penetration, slides along it when moving diagonally
  - step-up: climbs a 0.4 box, does NOT climb a 0.6 box
  - crouch under a low ceiling: cannot stand while under a 1.5 box
  - jump: rises then lands back onGround; gravity pulls to floor y=0 from height
  - raycastAABB/hitscan: hits body, headshot when aiming at head box, blocked by a solid in between
  - falloffMul: 1 below rangeStart, minDmgMul past rangeEnd, 0.5-ish midpoint (linear)
  - applySpread deterministic for a fixed seed; 0 spread returns input dir
- protocol.test.ts: accepts every valid C2S variant (assert deep-equal with clamping: moveX 2 → 1,
  pitch 9 → 1.45); rejects: unknown t, missing fields, NaN yaw, bad weapon id, overlong name,
  non-object; name trimmed; code uppercased.
- game.test.ts (fake RoomIO capturing sent messages):
  - two players join → phase becomes freeze (round 1) within a few ticks (drive room via start() and
    manual waiting OR expose nothing — use vi.useFakeTimers to advance)
  - buy rifle with $800 fails 'insufficient funds'; give money via a kill in warmup (feed crafted
    inputs + aim) OR call handleBuy during freeze with enough money granted by round rewards —
    assert buy_result ok and owned list per tryBuy semantics
  - elimination: with 1v1, kill the enemy (crafted fire inputs aimed at a stationary target with
    rttMs 0) → round_end event with correct winner and score
  - warmup: solo player phase stays warmup and respawns after death
  Keep tests deterministic (rttMs()=0, no real timers except where faked).

Definition of done: `npx vitest run` passes once S2/S3 exist; your tests compile NOW against the
frozen signatures (typecheck green even before implementations exist is NOT required — game.test.ts
may fail to compile until S2 lands; that's expected, note it in your return).
Return: summary, files touched, test list, risks. NOT file contents.
