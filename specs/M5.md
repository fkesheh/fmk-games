# Task M5 — map: bunker (underground CQB)
Owns (exclusive): `shared/src/maps/bunker.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + "Map specs" M5 entry + invariants), shared/src/maps/types.ts,
shared/src/maps/dustbowl.ts (reference), shared/src/palette.ts.

Replace placeholder CONTENTS with a full underground CQB map: 32×32, concreteDark/metalDark
corridors 2–2.5m wide, 4 rooms off a central hub, crate stacks, pipe runs along walls, ceiling
y≈2.8, darkest theme (ink fog ~0.018, hemiIntensity ≥0.75 so players stay readable).
Sightlines ≤25m. 40–90 boxes; deco pipe/barrel 30–80; ≥6 spawns/team.

Verify every invariant (enclosure + ceiling, ≥2 routes + loop, no spawn sightline, cover ≤8m,
corridors ≥1.4m, spawns valid). Keep `export const bunker: MapDef`.

Definition of done: `npm run typecheck` passes; invariants verified with reasoning.
Return: summary, box/deco counts, invariant checklist, typecheck output. NOT file contents.
