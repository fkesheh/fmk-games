# Task C2 — client input
Owns (exclusive): `client/src/input/input.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + C2 module table), shared/src/types.ts (INPUT_* bits).

Implement InputController per the frozen export table:
- Pointer lock on canvas click (start() attaches; stop() detaches everything). onLockChange fires.
- Mouse look: yaw/pitch from movementX/Y, sensitivity 0.0022 rad/px (×0.4 while setZoomed(true)),
  pitch clamped [-1.45, 1.45], yaw wrapped to [-π, π]. yaw=0 looks -Z; moving mouse right turns
  right (yaw decreases if your convention needs that — verify against aimDir(): forward = (-sin, -cos)).
- Held state: WASD (W = +moveZ forward, S = -moveZ, A = -moveX left? verify: moveX positive =
  strafe RIGHT per types.ts), Space jump, Ctrl or C crouch, LMB fire (INPUT_FIRE), RMB alt (INPUT_ALT,
  preventDefault context menu). frame() returns current held state, safe to call every rAF.
- Edges (queued, drained by edges()): R → reload; Digit1..6 → slot n; B → buy; Tab down/up →
  scoreboard (preventDefault); Esc → menu; wheel up/down → slot edge for prev/next (n wraps 1..6
  relative to last slot edge, default start 1).
- Robustness: window blur clears ALL held keys/buttons (no stuck movement); keyup/keydown use
  e.code not e.key; suppress default for game keys while locked.
- Semi-auto fire latch per CONTRACT.md: a fire press that begins and ends between two frame()
  samples must still be reported once — latch INPUT_FIRE until included in one frame() result.

Definition of done: `npm run typecheck` passes; no stubs.
Return: summary, files touched, typecheck output, risks. NOT file contents.
