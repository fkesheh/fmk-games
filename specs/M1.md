# Task M1 — map: crossfire (industrial yard)
Owns (exclusive): `shared/src/maps/crossfire.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + "Map specs" M1 entry + invariants list), shared/src/maps/types.ts,
shared/src/maps/dustbowl.ts (the reference — match its quality and its header-comment invariants),
shared/src/palette.ts.

Replace the placeholder file CONTENTS with a full industrial-yard map per the M1 spec:
56×40, concrete/metal world, warehouse block + loading dock (0.8 platform + step boxes of ≤0.4),
container stacks (metal, ~2.6 high, some double), pipe runs, pallet piles, mid gantry.
40–90 boxes, 40–100 deco props across zones (barrel/pallet/pipe), ≥6 spawns per team.

Verify every invariant in the dustbowl header comment (enclosure, 3 routes, no spawn-to-spawn
sightline, cover every ≤8m, sightlines ≤42m, corridor width ≥1.4m, spawns on y=0 outside boxes).
Trace a few sightlines by hand from spawn coordinates to prove them. Keep the same export name and
type (`export const crossfire: MapDef`).

Definition of done: `npm run typecheck` passes; invariants hold on paper (list your sightline/cover
reasoning in the return); no other files touched.
Return: summary, box/deco counts, invariant checklist, typecheck output. NOT file contents.
