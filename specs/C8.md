# Task C8 — HUD
Owns (exclusive): `client/src/ui/hud.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + C8 module table), UX_BIBLE.md (hierarchy, states, readability),
STYLE_BIBLE.md. Colors: read PALETTE values (import from @fps/shared) and set them as CSS custom
properties on your root; all styling inline-in-JS or a <style> block you inject (style.css is C11's
file — do NOT touch it).

Implement Hud per the frozen export table, DOM-based, pointer-events none, layered:
- Crosshair: 4 lines around center, gap = spreadPx (px), 2px white w/ dark outline for visibility on
  bright maps; hidden while scoped; scope overlay when scoped (radial black vignette, thin cross,
  circle edge).
- HP bottom-left (big number + 220px bar, hpGreen→danger under 30 with pulse), ammo bottom-right
  (mag huge / reserve small; mag 0 → danger + "R TO RELOAD" hint), money above ammo (hudAccent,
  green flash on increase), top-center timer (mm:ss) + phase label + "BUY" chip while canBuy,
  score "T n : m CT" flanking the timer, round number small beneath.
- Killfeed top-right (≤5 rows, newest top, 5s fade+remove): "killer [weaponIcon] victim", headshot
  adds a ✕/skull marker (text glyph is fine); weapon shown as its short name.
- hitmarker(headshot, killed): 4 diagonal ticks at center flash 120ms — white normally, danger red
  on headshot or kill.
- damageFrom(yawRelative): red arc segment on a ring around center pointing towards the source
  (yawRelative 0 = ahead), 0.8s fade.
- banner(title, sub): big center text (round start/end) fading after 2.5s; queue if busy.
- Dead/spectating: "SPECTATING name" center-bottom + subtle vignette.
- show(on) toggles the whole layer. All text ≥12px at 1080p; layout safe 1280×720 → 21:9.

Definition of done: `npm run typecheck` passes; no stubs.
Return: summary, files touched, typecheck output, risks. NOT file contents.
