# Task S3 — combat resolution + economy (pure logic)
Owns (exclusive): `server/src/combat.ts`, `server/src/economy.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + S3 section), shared/src/physics.ts (hitscan/applySpread/falloff/
aimDir), config.ts (WEAPONS, ECONOMY, shotSeed), types.ts.

Implement exactly per the frozen export table:
- `combat.ts`: LagBuffer ring buffer (store per-tick entries, at(tick, excludeId) returns the stored
  tick nearest to but not newer than the requested tick; empty → []). resolveShot: per pellet,
  direction = applySpread(aimDir, effectiveSpread, rng(seed + pelletIndex)) with
  effectiveSpread = (scoped && weapon.scopedSpreadDeg != null) ? scopedSpreadDeg : min(spreadDeg +
  bloomDeg, maxSpreadDeg); hitscan vs targets blocked by solids; maxDist = knife ? weapon.rangeEnd
  : a generous 200; dmg = max(1, round(damage × falloffMul(dist, …) × (headshot ? headshotMul : 1))).
  wallEndPoint for tracer visuals (nearest solid hit along dir, else origin + dir × maxDist).
- `economy.ts`: tryBuy per contract rules and exact reason strings ('buy time expired' /
  'not for sale' / 'already owned' / 'insufficient funds'); knife+pistol always owned, never
  buyable; primary replacement semantics; killReward and roundRewards with ECONOMY clamps applied
  by callers documented in comments; roundRewards returns the per-team gains for win/loss
  (winner null ⇒ both teams get loss reward).

Both modules are PURE (no I/O, no Date.now, no setInterval) except LagBuffer's constructor args.
Definition of done: `npm run typecheck` passes; no stubs.
Return: summary, files touched, typecheck output, risks. NOT file contents.
