# Task M2 — map: office (indoor)
Owns (exclusive): `shared/src/maps/office.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + "Map specs" M2 entry + invariants), shared/src/maps/types.ts,
shared/src/maps/dustbowl.ts (reference), shared/src/palette.ts.

Replace placeholder CONTENTS with a full indoor office map: 40×32, cubicle grid (1.1-high
partitions — chest-high cover), desk rows, server room (metal racks), corridor ring, 2 meeting
rooms, ceiling at y≈3.2 (thin box), mats: carpet/plaster/desk/paper/metal. Sightlines ≤25m.
40–90 boxes; deco paperStack/plant 40–100 props; ≥6 spawns/team.
Indoor maps: outer walls h≈3.5 + ceiling; theme = interior gloom (skyIndoor/charcoal fog,
hemiIntensity ≥0.7 so players stay clearly lit).

Verify every invariant (enclosure, ≥2 routes + loop, no spawn sightline, cover density, corridor
≥1.4m). Keep `export const office: MapDef`.

Definition of done: `npm run typecheck` passes; invariants verified with your reasoning.
Return: summary, box/deco counts, invariant checklist, typecheck output. NOT file contents.
