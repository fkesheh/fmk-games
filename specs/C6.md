# Task C6 — combat effects (pooled)
Owns (exclusive): `client/src/render/effects.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + C6 module table + FX spec), client/src/contract/visual.ts.

Implement Effects per the frozen export table:
- Tracers: pool of 64 thin stretched boxes (or Line2-free quad strips) in tracer color, additive
  blending allowed; each lives 60ms scaling/fading out; tracer(from,to) orients one along the ray.
- Particles: one THREE.Points pool of 256 with per-particle velocity/gravity/life in preallocated
  Float32Arrays; spawn functions write into dead slots: impact = 8 particles (concrete grey, 2
  muzzle-orange sparks, short life, gravity), blood = 6 (blood red), death = 12 (team color).
  Points material: vertexColors, size ~0.06, sizeAttenuation, depthWrite false.
- update(dt): advance pools, hide dead slots by zeroing alpha/position; zero allocation after the
  pools are built. clear() resets all.
- Everything PALETTE colors; no new materials per call.

Definition of done: `npm run typecheck` passes; no stubs.
Return: summary, files touched, typecheck output, risks. NOT file contents.
