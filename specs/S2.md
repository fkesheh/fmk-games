# Task S2 — authoritative game room
Owns (exclusive): `server/src/game.ts`
Depends on: contract only. Do NOT import server/src/net.ts or rooms.ts — you get RoomIO injected.

Read first: CONTRACT.md (RULES + S2/S3 sections + behavioral invariants), shared/src/types.ts,
config.ts, physics.ts, maps/dustbowl.ts (example of the map data you consume via MAPS).

Implement `GameRoom` exactly per the frozen export table + ALL behavioral invariants in CONTRACT.md:
- Phase machine warmup→freeze→live→roundEnd→(halftime)→matchEnd→warmup, all timings from ROUNDS
  config, phaseEndsAt broadcast in serverTime ms.
- Per-player state: BodyState (shared physics), input queue, weapons owned + current + mag/reserve,
  reload-until, money, kills/deaths, alive, bloom (current spread add), shotSeq, spawn-protected-until,
  respawnAt, spectateTarget, lastInputAt, lastProcessedSeq (ack).
- Movement: consume ≤ NET.maxInputPerTick queued inputs per tick, stepBody each at TICK_DT with the
  held weapon's moveMul. Fire handling per INPUT_FIRE edge (semi) / hold (auto) with weapon interval,
  mag bookkeeping, reload timing (switch cancels), scoped state from INPUT_ALT. resolveShot via
  LagBuffer rewound by clamp(rttMs/2 → ticks, 0..lagCompMaxMs) — import combat/economy from S3 per
  the module table. shotSeed(tick, shotSeq) for spread.
- Damage/death: hp, kill events (killer, weapon, headshot), dmg_taken (yaw towards shooter),
  killReward to killer, warmup respawn timer, live-round spectator assignment, spawn protection.
- Round flow: elimination/time(tie⇒CT)/forfeit win conditions, rewards, halftime swap, match end
  conditions, full reset to warmup 6s after match_end.
- Spawns: random team spawn, ≤4 retries avoiding enemies <10m; freeze teleports + heals.
- Snapshots every tick to every player (players list, per-recipient you + ack). Events broadcast
  (hit/dmg_taken only to the involved players).
- stalePlayers(): no input for NET.inputTimeoutMs.
- Zero allocation in the tick hot path where practical (reuse snapshot arrays); never throws.

Definition of done: `npm run typecheck` passes; all invariants implemented; no stubs.
Return: summary, files touched, typecheck output, risks. NOT file contents.
