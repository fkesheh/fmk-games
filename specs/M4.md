# Task M4 — map: urbana (old town)
Owns (exclusive): `shared/src/maps/urbana.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + "Map specs" M4 entry + invariants), shared/src/maps/types.ts,
shared/src/maps/dustbowl.ts (reference), shared/src/palette.ts.

Replace placeholder CONTENTS with a full old-town map: 56×44, brick/plaster building blocks (solid,
no interiors) forming a central street + two alleys + a plaza, market crates/barrels, a cart prop
built from boxes (box bed + cyl wheels via crate/wood mats), roofRed caps on buildings for skyline
(visual only, unreachable), mats brick/plaster/concrete/crate/roofRed.
40–90 boxes; deco barrel/shrub 40–100; ≥6 spawns/team.

Verify every invariant (enclosure, 3 routes, no spawn sightline, cover ≤8m, sightlines ≤42m,
corridors ≥1.4m, spawns valid). Keep `export const urbana: MapDef`.

Definition of done: `npm run typecheck` passes; invariants verified with reasoning.
Return: summary, box/deco counts, invariant checklist, typecheck output. NOT file contents.
