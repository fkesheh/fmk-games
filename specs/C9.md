# Task C9 — menus (main/buy/scoreboard/match end)
Owns (exclusive): `client/src/ui/menus.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + C9 module table), UX_BIBLE.md (states + onboarding), DESIGN_BIBLE.md
(economy — buy menu must surface price vs role). Colors from PALETTE (import from @fps/shared) as
CSS custom properties on your root; inject your own <style> (style.css is C11's — do NOT touch it).

Implement Menus per the frozen export table:
- showMain(errorText?): centered panel on dim backdrop — game title "STRICKEN", name input
  (persist to localStorage, default "Player"), primary Quick Join button, two-column section:
  Create Private (6-map picker grid with map names, then Create) and Join Private (5-char code
  input, uppercase, Join), public room list below (rows: map name, players/10, phase; click to join;
  Refresh button; empty state per UX_BIBLE). Error line in danger red when errorText set. Controls
  card at the bottom (WASD/mouse/LMB/R/B/Tab/1-6/Esc per UX_BIBLE).
- showInRoom(label, code): small non-modal chip top-left: "label · code XXXXX (share)". 
- showBuy(money, owned, canBuy): centered panel — money display; cards for smg/shotgun/rifle/sniper:
  name, price, 3 stats (damage, fire interval as RPM, mag), one-line role (from DESIGN_BIBLE weapon
  intent); owned card highlighted; unaffordable or !canBuy → disabled at 40% opacity; knife+pistol
  row marked "ISSUED". Click buys (cb.onBuy). Close button + B/Esc handled by clientGame (you expose
  hideBuy; the key edge comes from C10). Can be re-opened after purchase (money updates).
- showScoreboard: two team tables (T amber header / CT blue header, labels not just colors): name,
  kills, deaths, money (own row only), alive dot; you row highlighted; score header T n : m CT.
- showMatchEnd(winner, scoreT, scoreCT, youTeam, roster): fullscreen "VICTORY"/"DEFEAT" (youTeam
  null → "MATCH OVER"), final score, top-3 players by kills from roster, auto-dismiss by caller.
- showJoining(): dim overlay "Joining…" (required state). showPause(): in-room Esc surface with
  Resume (cb.onResume) and Leave Room (cb.onLeave) — NOT the main menu.
- hideAll() hides everything. Menus are modal overlays (pointer-events auto) except showInRoom chip.

Definition of done: `npm run typecheck` passes; no stubs.
Return: summary, files touched, typecheck output, risks. NOT file contents.
