# Task M3 — map: frostbite (snowfield)
Owns (exclusive): `shared/src/maps/frostbite.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + "Map specs" M3 entry + invariants), shared/src/maps/types.ts,
shared/src/maps/dustbowl.ts (reference), shared/src/palette.ts.

Replace placeholder CONTENTS with a full snowfield map: 60×44, ice ridge lanes, rock clusters,
frozen creek gully (0.6-deep trench, ice floor, walkable via ≤0.4 step boxes), snowdrifts (low wide
snow boxes as soft cover), mats snow/ice/rock, cold theme (skyCold/fogCold, fogDensity ~0.01).
40–90 boxes; deco snowRock/shrub 40–100; ≥6 spawns/team.

Verify every invariant (enclosure, 3 routes, no spawn sightline, cover ≤8m, sightlines ≤42m,
corridors ≥1.4m, spawns valid). Keep `export const frostbite: MapDef`.

Definition of done: `npm run typecheck` passes; invariants verified with reasoning.
Return: summary, box/deco counts, invariant checklist, typecheck output. NOT file contents.
