# Task C11 — app shell + boot + debug surface
Owns (exclusive): `client/src/main.ts`, `client/src/style.css`, `client/index.html`
Depends on: contract only. Code against the CONTRACT.md module table for C8/C9/C10 (their files may
not exist yet — the exports are frozen).

Read first: CONTRACT.md (RULES + C11 + debug surface), UX_BIBLE.md.

Implement:
- index.html: dark full-viewport canvas (#game), overlay roots #hud (pointer-events none) and
  #menu, #app container, module script src /src/main.ts, title "STRICKEN", meta viewport, no
  external assets/fonts (system font stack).
- style.css: reset-ish base, #app fixed inset 0 overflow hidden background ink (#14171c), canvas
  block w/h 100%, #hud/#menu absolute inset 0, #menu pointer-events auto, cursor rules (crosshair
  none while locked handled by pointer lock itself), a .debug-overlay style (mono, top-right,
  translucent). Keep it minimal — C8/C9 inject their own component styles.
- main.ts: boot — create ClientState, Hud(#hud), Menus(#menu, callbacks wired to ClientGame),
  ClientGame({canvas, hud, menus, state}); menus.showMain() on boot; rAF loop calling
  game.frame(now) with dt clamp; window resize → game handles via SceneRig.resize (call through
  frame or a listener); first pointerdown/keydown → audio resume happens inside ClientGame's engine
  (call game.resumeAudio() if present — if not in the table, create the AudioEngine in main and
  pass it? NO: contract has AudioEngine constructed inside ClientGame — so main dispatches a custom
  'fps:gesture' window event on first gesture and ClientGame listens for it — document this in your
  code; it's part of the C10/C11 seam). Debug: when location.search includes debug, show overlay
  with fps/ping/pos/phase updated 4Hz. Expose window.__fps EXACTLY per the CONTRACT.md debug
  surface, delegating to game + input (game exposes what it needs to through its public methods;
  the debug.press path sets button state through the InputController — add a small public method on
  ClientGame if needed, e.g. debugSetButton(btn, down), debugSetLook(yaw,pitch), debugSetMove(x,z),
  and document them in code; main.ts may use them).
- Error guard: window.onerror shows a small red banner (never a silent white screen).

Definition of done: `npm run typecheck` passes; `npm run build` produces a client bundle; no stubs.
Return: summary, files touched, typecheck output, risks. NOT file contents.
