# Task C4 — player models + animation
Owns (exclusive): `client/src/render/playerModels.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + C4 module table + "Soldier" model sheet), STYLE_BIBLE.md,
client/src/contract/visual.ts. You may import makeWeaponModel from './viewModel.js' (contract-allowed).

Implement PlayerModels per the frozen export table:
- Soldier factory per the CONTRACT.md model sheet (15–25 prims): torso, head+helmet, 2 arm pivots
  holding the current weapon, 2 leg pivots, team-colored (CT ctBlue/ctDark, T tAmber/tBrown),
  nameplate sprite (canvas texture: player name, white text on team-color-tinted translucent ink
  strip, ~0.35u above head, always faces camera). Update nameplate only on create (not per frame).
- sync(): create models for new ids, remove gone ids (dispose their geometries/groups — the shared
  unit geometries from factories are cached, so dispose only nameplate textures/materials), hide the
  localId model entirely. Per frame per model: position at feet, yaw on the group; walk swing
  (legs/arms ±25° sin, phase advanced by horizontal speed × dt × 2.2) when moving else ease to idle
  breathe (torso scaleY ±0.02 sin 2Hz); crouch pose (legs scaleY 0.6, group y offset −0.35);
  pitch applied to the arm/weapon pivot only (±0.6 rad clamp). Death: rotate group to lying
  (rotation.x → -π/2) over 0.4s, then sink 1u over 1s starting at 2s; keep corpse 3s total.
- muzzle(id): flash quad at gun tip (emissive muzzle color, 2 crossed planes, 50ms).
- Weapon in hands: swap when snap.weapon changes (rebuild only that sub-group).
- Reuse models/groups; no per-frame allocation (keep temp vectors module-level).

Definition of done: `npm run typecheck` passes; no stubs.
Return: summary, files touched, typecheck output, risks. NOT file contents.
