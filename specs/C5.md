# Task C5 — first-person viewmodel + weapon models
Owns (exclusive): `client/src/render/viewModel.ts`
Depends on: contract only.

Read first: CONTRACT.md (RULES + C5 module table + weapon model sheets), STYLE_BIBLE.md,
client/src/contract/visual.ts.

Implement per the frozen export table:
- makeWeaponModel(id): 8–20 prims per weapon, distinctive silhouettes exactly per the CONTRACT.md
  model sheets (knife blade+grip, pistol, smg stubby + curved mag, shotgun tubes + wood pump,
  rifle long barrel + curved wood mag, sniper long thin barrel + scope tube). Palette colors only.
  Origin at the grip, barrel towards -Z (so it aims where the camera looks).
- ViewModel: group attached to the camera (camera.add). Weapon sits bottom-right (~x 0.28, y −0.26,
  z −0.5, scale 0.6) with simple gloved-hand boxes (charcoal) gripping it. setWeapon: 0.25s
  lower-then-raise swap. update(dt, moving, scoped): walk bob (y = sin(t×9)×0.012, x =
  cos(t×4.5)×0.008 when moving; gentler idle sway otherwise) + hide entirely when scoped.
  fire(): recoil kick — position z += 0.03 and rotation.x -= 0.03 recovering with a spring
  (stiffness ~180, damping ~14); plus muzzle flash: 2 crossed emissive quads (muzzle color) at the
  barrel tip for 40ms. reload(durSec): dip down 0.12u + tilt 25° over 20% of dur, hold, return.
- No per-frame allocation; keep temps module-level; dispose nothing shared.

Definition of done: `npm run typecheck` passes; no stubs.
Return: summary, files touched, typecheck output, risks. NOT file contents.
